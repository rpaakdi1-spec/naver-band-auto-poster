# Windows 배치 파일 인코딩 문제 해결 완료

## 🐛 발생한 문제

Windows 명령 프롬프트에서 배치 파일 실행 시 다음과 같은 오류 발생:

```
'C:\Program'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는 배치 파일이 아닙니다.
'踰꾧퉭'은(는) 내부 또는 외부 명령...
```

## 🔍 원인

1. **UTF-8 인코딩**: 배치 파일이 UTF-8로 저장되어 한글이 깨짐
2. **이모지 사용**: ✅❌⚠️ 등의 이모지가 CP949 인코딩에서 지원되지 않음
3. **경로 따옴표**: Chrome 경로에 공백이 있어 따옴표 필요

## ✅ 해결 방법

### 1. 파일 재생성
- 모든 이모지를 텍스트 레이블로 교체
  - ✅ → `[확인]` 또는 `[OK]`
  - ❌ → `[오류]` 또는 `[ERROR]`
  - ⚠️ → `[경고]` 또는 `[WARNING]`

### 2. 인코딩 변환
- UTF-8 → CP949 (Windows 한글 인코딩)
- Python 스크립트로 자동 변환 완료

### 3. 경로 문제 수정
- Chrome 경로를 따옴표로 감싸기
```batch
set CHROME_PATH="C:\Program Files\Google\Chrome\Application\chrome.exe"
```

## 📦 수정된 파일

### 한글 버전 (CP949 인코딩)
- ✅ `start_chrome_debug.bat` - 이모지 제거, CP949 인코딩
- ✅ `run_safe_macro.bat` - 이모지 제거, CP949 인코딩

### 영어 버전 (인코딩 무관)
- ✅ `start_chrome_debug_en.bat` - 영어 버전 (인코딩 문제 없음)

## 🚀 이제 사용 가능합니다!

### 한글 버전 사용
```cmd
start_chrome_debug.bat
```

### 영어 버전 사용 (인코딩 문제 없음)
```cmd
start_chrome_debug_en.bat
```

## 🔧 수동 수정 방법 (참고용)

만약 다른 파일에서 같은 문제가 발생하면:

### 방법 1: 메모장으로 수정
1. 파일을 메모장으로 열기
2. `파일 > 다른 이름으로 저장`
3. 인코딩: `ANSI` 선택
4. 저장

### 방법 2: VS Code로 수정
1. VS Code에서 파일 열기
2. 우측 하단 인코딩 클릭
3. `Reopen with Encoding` → `Korean (Windows 949)`
4. `Save with Encoding` → `Korean (Windows 949)`
5. 저장

### 방법 3: Python 스크립트
```python
# encoding_fix.py
with open('your_file.bat', 'r', encoding='utf-8') as f:
    content = f.read()

# 이모지 제거
content = content.replace('✅', '[OK]')
content = content.replace('❌', '[ERROR]')
content = content.replace('⚠️', '[WARNING]')

with open('your_file.bat', 'w', encoding='cp949') as f:
    f.write(content)
```

## 📝 앞으로 주의할 점

### Windows 배치 파일 작성 시
1. **인코딩**: CP949 (ANSI) 사용
2. **이모지**: 사용하지 않기
3. **경로**: 공백이 있으면 따옴표로 감싸기
4. **테스트**: 실제 Windows 환경에서 테스트

### 다국어 지원이 필요하면
- 영어 버전 별도 제공 권장
- PowerShell 스크립트 사용 고려 (UTF-8 지원)

## 🎉 완료!

이제 배치 파일이 정상적으로 작동합니다!

```cmd
# Chrome 디버깅 모드 실행
start_chrome_debug.bat

# 또는 영어 버전
start_chrome_debug_en.bat

# 매크로 실행
run_safe_macro.bat
```

---

## 🔗 관련 커밋

```
60840e7 fix: Fix Windows batch file encoding issue
```

모든 수정사항이 Git에 커밋되고 푸시되었습니다!
