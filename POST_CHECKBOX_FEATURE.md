# ✅ 포스트 체크박스 기능 추가 완료

## 📋 목차
- [요청 사항](#요청-사항)
- [구현 내용](#구현-내용)
- [변경 사항](#변경-사항)
- [사용 방법](#사용-방법)
- [커밋 정보](#커밋-정보)

---

## 🎯 요청 사항

**사용자 요청:**
```
등록된 포스트 에도 체크박스를 만들어줘
```

**목적:**
- 등록된 포스트를 활성화/비활성화할 수 있는 체크박스 추가
- 채팅방과 동일하게 포스트도 선택적으로 사용 가능하도록 개선

---

## ✅ 구현 내용

### **1. GUI 버전 (`src/gui.py`)**

#### **Before (기존)**
```python
# Listbox 사용 - 단순 텍스트 목록
self.post_listbox = tk.Listbox(post_list_frame, height=20)
self.post_listbox.pack(side=tk.LEFT, fill=tk.BOTH, expand=True)

# 포스트 추가 시
self.post_listbox.insert(tk.END, f"✓ {display_content}")

# 포스트 삭제 시
selection = self.post_listbox.curselection()
self.post_listbox.delete(index)
```

**문제점:**
- ❌ 체크박스 없음
- ❌ 활성/비활성 제어 불가
- ❌ 개별 삭제 버튼 없음

---

#### **After (개선)**
```python
# Canvas + Frame 사용 - 동적 체크박스 목록
post_canvas = tk.Canvas(post_list_frame, height=300)
self.post_checkboxes_frame = ttk.Frame(post_canvas)

# 포스트 위젯 추적
self.post_widgets = []
self.post_check_vars = []

# 각 포스트마다 체크박스 + 내용 + 삭제 버튼
for i, post in enumerate(posts):
    frame = ttk.Frame(self.post_checkboxes_frame)
    
    # 체크박스
    var = tk.BooleanVar(value=post.get('enabled', True))
    checkbox = ttk.Checkbutton(frame, variable=var, command=lambda idx=i: self.toggle_post(idx))
    
    # 내용 표시
    label = ttk.Label(frame, text=display_content)
    
    # 개별 삭제 버튼
    delete_btn = ttk.Button(frame, text="🗑", command=lambda idx=i: self.delete_single_post(idx))
```

**개선점:**
- ✅ 각 포스트마다 체크박스
- ✅ 실시간 활성/비활성 제어
- ✅ 개별 삭제 버튼
- ✅ 스크롤 지원

---

### **2. Streamlit 버전 (`streamlit_app.py`)**

**이미 구현되어 있음!**

```python
for idx, post in enumerate(posts):
    col_check, col_info, col_del = st.columns([0.5, 3, 0.5])
    
    with col_check:
        enabled = st.checkbox(
            "활성",
            value=post.get('enabled', True),
            key=f"post_{idx}",
            label_visibility="collapsed"
        )
        post['enabled'] = enabled
    
    with col_info:
        st.text(content)  # 줄바꿈 유지
    
    with col_del:
        if st.button("🗑", key=f"del_post_{idx}"):
            st.session_state.poster.config['posts'].pop(idx)
            st.rerun()
```

**특징:**
- ✅ 체크박스로 활성/비활성 제어
- ✅ 줄바꿈 유지하여 표시 (최대 3줄)
- ✅ 개별 삭제 버튼
- ✅ 실시간 업데이트

---

## 📝 변경 사항

### **src/gui.py**

#### **1. UI 구조 변경**
```python
# 기존: Listbox
self.post_listbox = tk.Listbox(...)

# 변경: Canvas + Frame (스크롤 가능)
post_canvas = tk.Canvas(post_list_frame, height=300)
self.post_checkboxes_frame = ttk.Frame(post_canvas)

# 위젯 추적 리스트
self.post_widgets = []
self.post_check_vars = []
```

---

#### **2. add_post() 메소드**
```python
# 기존
def add_post(self):
    # ...
    self.post_listbox.insert(tk.END, f"✓ {display_content}")

# 변경
def add_post(self):
    # ...
    self.refresh_post_list()  # 전체 목록 새로고침
```

---

#### **3. remove_post() 메소드**
```python
# 기존
def remove_post(self):
    selection = self.post_listbox.curselection()
    if not selection:
        messagebox.showwarning("경고", "삭제할 포스트를 선택하세요.")
        return
    
    index = selection[0]
    self.poster.config['posts'].pop(index)
    self.post_listbox.delete(index)

# 변경
def remove_post(self):
    # 체크된 포스트들 찾기
    selected_indices = []
    for i, var in enumerate(self.post_check_vars):
        if var.get():
            selected_indices.append(i)
    
    if not selected_indices:
        messagebox.showwarning("경고", "삭제할 포스트를 체크하세요.")
        return
    
    # 역순으로 삭제
    for index in reversed(selected_indices):
        self.poster.config['posts'].pop(index)
    
    # 목록 새로고침
    self.refresh_post_list()
```

---

#### **4. 새로운 메소드 추가**

##### **refresh_post_list()**
```python
def refresh_post_list(self):
    """포스트 목록 UI 새로고침"""
    # 기존 위젯 제거
    for widget in self.post_widgets:
        widget.destroy()
    self.post_widgets.clear()
    self.post_check_vars.clear()
    
    # 포스트 목록 다시 그리기
    posts = self.poster.config.get('posts', [])
    for i, post in enumerate(posts):
        frame = ttk.Frame(self.post_checkboxes_frame)
        frame.pack(fill=tk.X, padx=5, pady=2)
        
        var = tk.BooleanVar(value=post.get('enabled', True))
        self.post_check_vars.append(var)
        
        # 체크박스
        checkbox = ttk.Checkbutton(
            frame, 
            variable=var,
            command=lambda idx=i: self.toggle_post(idx)
        )
        checkbox.pack(side=tk.LEFT)
        
        # 포스트 내용 표시
        content = post['content']
        display_content = content.replace('\n', '\\n')
        if len(display_content) > 80:
            display_content = display_content[:80] + "..."
        
        label = ttk.Label(frame, text=display_content)
        label.pack(side=tk.LEFT, fill=tk.X, expand=True, padx=5)
        
        # 개별 삭제 버튼
        delete_btn = ttk.Button(
            frame,
            text="🗑",
            width=3,
            command=lambda idx=i: self.delete_single_post(idx)
        )
        delete_btn.pack(side=tk.RIGHT)
        
        self.post_widgets.extend([frame, checkbox, label, delete_btn])
```

##### **toggle_post()**
```python
def toggle_post(self, index):
    """포스트 활성/비활성 토글"""
    if index < len(self.poster.config['posts']):
        enabled = self.post_check_vars[index].get()
        self.poster.config['posts'][index]['enabled'] = enabled
        status = "활성화" if enabled else "비활성화"
        self.log(f"{'✅' if enabled else '⏸'} 포스트 {status}: 인덱스 {index}")
```

##### **delete_single_post()**
```python
def delete_single_post(self, index):
    """개별 포스트 삭제"""
    if index < len(self.poster.config['posts']):
        content_preview = self.poster.config['posts'][index]['content'][:30]
        self.poster.config['posts'].pop(index)
        self.log(f"🗑 포스트 삭제: {content_preview}...")
        self.refresh_post_list()
```

---

#### **5. load_config() 메소드**
```python
# 기존
for post in config['posts']:
    if post.get('enabled', True):
        content = post['content']
        display_content = content[:50] + "..." if len(content) > 50 else content
        self.post_listbox.insert(tk.END, f"✓ {display_content}")

# 변경
self.refresh_post_list()  # 전체 목록 새로고침
```

---

## 🎨 UI 레이아웃

### **GUI 버전**

```
┌─────────────────────────────────────────┐
│ ✓ 등록된 포스트:                        │
├─────────────────────────────────────────┤
│ ┌─────────────────────────────────────┐ │
│ │ ☑ 첫 번째 포스트 내용...         🗑 │ │
│ │ ☑ 두 번째 포스트 내용...         🗑 │ │
│ │ ☐ 세 번째 포스트 내용... (비활성) 🗑 │ │
│ │ ☑ 네 번째 포스트 내용...         🗑 │ │
│ └─────────────────────────────────────┘ │
│              ↕ 스크롤 가능               │
└─────────────────────────────────────────┘
```

**특징:**
- ✅ 체크박스: 활성/비활성 제어
- ✅ 내용 표시: 줄바꿈을 `\n`으로 표시 (80자 제한)
- ✅ 개별 삭제: 각 포스트마다 🗑 버튼
- ✅ 스크롤: 많은 포스트도 표시 가능

---

### **Streamlit 버전**

```
┌─────────────────────────────────────────┐
│ ✓ 등록된 포스트                         │
├─────────────────────────────────────────┤
│ [✓] 첫 번째 줄                       🗑 │
│     두 번째 줄                           │
│     세 번째 줄                           │
│                                          │
│ [✓] 짧은 포스트                      🗑 │
│                                          │
│ [ ] 비활성화된 포스트 (실행 안 됨)  🗑 │
└─────────────────────────────────────────┘
```

**특징:**
- ✅ 체크박스: 실시간 활성/비활성 제어
- ✅ 줄바꿈 유지: 실제 줄바꿈으로 표시 (최대 3줄)
- ✅ 개별 삭제: 🗑 버튼
- ✅ 자동 스크롤: 많은 포스트 지원

---

## 🚀 사용 방법

### **GUI 버전 (src/gui.py)**

#### **1. 포스트 추가**
```
1. "포스트 내용:" 입력창에 내용 입력
2. 엔터키로 줄바꿈 가능
3. [✚ 추가] 버튼 클릭
4. 체크박스와 함께 목록에 추가됨
```

#### **2. 포스트 활성/비활성**
```
1. 등록된 포스트의 체크박스 클릭
2. ☑ 체크: 포스팅에 사용
3. ☐ 해제: 포스팅에서 제외
4. 로그에 "✅ 포스트 활성화" 또는 "⏸ 포스트 비활성화" 표시
```

#### **3. 개별 포스트 삭제**
```
1. 삭제할 포스트의 🗑 버튼 클릭
2. 해당 포스트만 삭제됨
3. 로그에 "🗑 포스트 삭제: 내용..." 표시
```

#### **4. 선택된 포스트 일괄 삭제**
```
1. 삭제할 포스트들을 체크
2. [✖ 삭제] 버튼 클릭
3. 체크된 모든 포스트 삭제
```

---

### **Streamlit 버전 (streamlit_app.py)**

#### **1. 포스트 추가**
```
1. "포스트 내용" 텍스트 영역에 입력
2. 엔터키로 줄바꿈 가능
3. [✚ 추가] 버튼 클릭
4. 체크박스와 함께 목록에 추가됨
```

#### **2. 포스트 활성/비활성**
```
1. 포스트 왼쪽의 체크박스 클릭
2. [✓] 체크: 포스팅에 사용
3. [ ] 해제: 포스팅에서 제외
4. 즉시 적용됨 (자동 저장)
```

#### **3. 개별 포스트 삭제**
```
1. 삭제할 포스트의 🗑 버튼 클릭
2. 해당 포스트만 삭제됨
3. 로그에 "🗑 포스트 삭제" 표시
```

---

## 💡 활용 예시

### **시나리오 1: 일부 포스트만 사용**

**상황:** 5개 포스트 중 3개만 사용하고 싶음

**방법:**
```
포스트 1 [✓] "안녕하세요"               → 사용
포스트 2 [ ] "점심 메뉴 추천"           → 제외
포스트 3 [✓] "날씨 좋네요"             → 사용
포스트 4 [ ] "주말 계획"               → 제외
포스트 5 [✓] "좋은 하루 되세요"        → 사용
```

**결과:** 포스트 1, 3, 5만 순환/랜덤으로 포스팅됨

---

### **시나리오 2: 계절별 포스트 관리**

**상황:** 여름/겨울 포스트를 구분해서 사용

**방법:**
```
# 여름 시즌
포스트 1 [✓] "시원한 여름 되세요"      → 활성화
포스트 2 [✓] "더위 조심하세요"         → 활성화
포스트 3 [ ] "따뜻한 겨울 보내세요"    → 비활성화
포스트 4 [ ] "추위 조심하세요"         → 비활성화

# 겨울 시즌 (체크박스만 변경)
포스트 1 [ ] "시원한 여름 되세요"      → 비활성화
포스트 2 [ ] "더위 조심하세요"         → 비활성화
포스트 3 [✓] "따뜻한 겨울 보내세요"    → 활성화
포스트 4 [✓] "추위 조심하세요"         → 활성화
```

**장점:** 포스트 삭제하지 않고 체크박스만 변경

---

### **시나리오 3: 테스트 포스트**

**상황:** 새로운 포스트를 테스트하고 싶음

**방법:**
```
포스트 1 [✓] "기존 포스트 1"          → 사용
포스트 2 [✓] "기존 포스트 2"          → 사용
포스트 3 [ ] "테스트 중인 포스트"     → 테스트 시에만 체크
```

**장점:** 테스트 포스트를 삭제하지 않고 보관 가능

---

## 📊 비교표

| 항목 | 기존 (Listbox) | 개선 (Checkboxes) |
|------|---------------|-------------------|
| **체크박스** | ❌ 없음 | ✅ 각 포스트마다 |
| **활성/비활성** | ❌ 불가 | ✅ 실시간 제어 |
| **개별 삭제** | ❌ 없음 | ✅ 🗑 버튼 |
| **일괄 삭제** | ❌ 1개만 | ✅ 체크된 항목 모두 |
| **스크롤** | ✅ 지원 | ✅ 지원 |
| **줄바꿈 표시** | `\n` 표시 | `\n` 표시 (GUI), 실제 줄바꿈 (Streamlit) |
| **실시간 업데이트** | ❌ 없음 | ✅ 즉시 적용 |

---

## 📁 변경된 파일

### **src/gui.py**

**변경 라인:** 약 150줄

**주요 변경:**
1. UI 구조: `Listbox` → `Canvas + Frame`
2. `add_post()`: `refresh_post_list()` 호출
3. `remove_post()`: 체크된 항목 일괄 삭제
4. `refresh_post_list()`: 신규 메소드 (포스트 목록 새로고침)
5. `toggle_post()`: 신규 메소드 (활성/비활성 토글)
6. `delete_single_post()`: 신규 메소드 (개별 삭제)
7. `load_config()`: `refresh_post_list()` 호출

---

### **streamlit_app.py**

**변경:** 없음 (이미 구현되어 있음)

**기존 코드:**
- ✅ 체크박스로 활성/비활성 제어
- ✅ 개별 삭제 버튼
- ✅ 줄바꿈 유지 표시 (최대 3줄)

---

## 📦 커밋 정보

### **커밋 해시**
```
(커밋 후 업데이트 예정)
```

### **커밋 메시지**
```
feat: Add checkboxes to post list for enable/disable control

- Change post UI from Listbox to Canvas+Frame with checkboxes
- Add refresh_post_list() method for dynamic post list rendering
- Add toggle_post() method for enable/disable control
- Add delete_single_post() method for individual post deletion
- Update remove_post() to delete multiple checked posts
- Each post now has: checkbox, content label, delete button
- Streamlit version already has this feature
- Improves post management flexibility
```

### **변경된 파일**
```
modified:   src/gui.py
new file:   POST_CHECKBOX_FEATURE.md
```

---

## 🎯 핵심 개선 사항

### **1. 사용성**
- ✅ 체크박스로 직관적인 활성/비활성 제어
- ✅ 개별 삭제 버튼으로 편리한 관리
- ✅ 일괄 삭제 기능 (체크된 항목)

### **2. 유연성**
- ✅ 포스트 삭제 없이 일시적 비활성화 가능
- ✅ 계절별, 상황별 포스트 관리 용이
- ✅ 테스트 포스트 관리 편리

### **3. 일관성**
- ✅ 채팅방 목록과 동일한 UI 패턴
- ✅ GUI와 Streamlit 버전 모두 체크박스 지원
- ✅ 통일된 사용자 경험

---

## 📚 관련 문서

1. **MULTILINE_POST_IMPROVEMENT.md** - 멀티라인 입력 개선
2. **FIX_TRIPLE_POSTING.md** - 줄바꿈 처리 수정
3. **COPY_PASTE_GUIDE.md** - 복사/붙여넣기 가이드
4. **DUPLICATE_URL_PREVENTION.md** - 중복 URL 방지
5. **README.md** - 프로젝트 개요

---

## 📌 상태

- ✅ **완료**
- 🗓️ **최종 업데이트**: 2026-01-23
- 🔗 **저장소**: https://github.com/rpaakdi1-spec/naver-band-auto-poster
- 📝 **버전**: v5.2.4

---

## 🎯 핵심 메시지

> ✅ **등록된 포스트에 체크박스가 추가되었습니다!**
> 
> **이제 다음 기능을 사용할 수 있습니다:**
> 
> 1. ✅ 체크박스로 포스트 활성/비활성 제어
> 2. ✅ 개별 삭제 버튼 (🗑)으로 편리한 관리
> 3. ✅ 일괄 삭제 (체크된 포스트들)
> 4. ✅ 채팅방과 동일한 직관적 UI
> 
> **활용 방법:**
> - 계절별 포스트를 삭제하지 않고 체크만 해제
> - 테스트 포스트를 보관하면서 일시적으로 비활성화
> - 선택적으로 원하는 포스트만 사용

---

**작성자**: AI Assistant  
**날짜**: 2026-01-23  
**문서 버전**: 1.0
