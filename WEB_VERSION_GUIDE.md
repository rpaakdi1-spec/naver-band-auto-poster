# 🌐 웹 버전 사용 가이드

## 🎉 웹 버전 출시!

**Streamlit 기반 웹 애플리케이션**으로 브라우저에서 바로 사용할 수 있습니다!

---

## ✨ 웹 버전의 장점

### 1. 브라우저에서 실행
- ✅ 별도의 프로그램 설치 불필요
- ✅ 모던한 웹 UI
- ✅ 실시간 업데이트

### 2. 크로스 플랫폼
- ✅ Windows, Mac, Linux 모두 지원
- ✅ 브라우저만 있으면 실행 가능

### 3. 간편한 사용
- ✅ 직관적인 UI
- ✅ 실시간 로그 표시
- ✅ 카운트다운 자동 업데이트

---

## 🚀 빠른 시작

### 방법 1: 배치 파일 (Windows)

```bash
run_web.bat
```

더블클릭만 하면 자동으로:
1. Streamlit 설치 (필요한 경우)
2. 웹 서버 시작
3. 브라우저 자동 열림

### 방법 2: Python 스크립트

```bash
python run_web.py
```

### 방법 3: 직접 실행

```bash
# Streamlit 설치
pip install streamlit

# 실행
streamlit run streamlit_app.py
```

---

## 📱 접속 방법

실행 후 자동으로 브라우저가 열립니다.

**로컬 주소:**
```
http://localhost:8501
```

**네트워크 주소:** (다른 기기에서 접속)
```
http://192.168.x.x:8501
```

---

## 🎯 사용 방법

### 1. 웹 서버 시작

```bash
run_web.bat
```

### 2. 브라우저 열기

자동으로 열리지 않으면:
```
http://localhost:8501
```

### 3. 채팅방 추가

**좌측 패널:**
1. 별명 입력 (예: "메인 채팅방")
2. 채팅방 URL 입력
3. [✚ 추가] 버튼 클릭

### 4. 포스트 추가

**우측 패널:**
1. 포스트 내용 입력
2. [✚ 추가] 버튼 클릭

### 5. 스케줄 설정

**사이드바 (왼쪽):**
- 포스팅 간격: 30분 (권장)
- 랜덤 딜레이: 5분
- 시작 일시: 원하는 시작 시간
- 종료 일시: 원하는 종료 시간

### 6. 설정 저장

사이드바 하단:
- [💾 설정 저장] 버튼 클릭

### 7. 실행

하단 버튼:
- **[▶ 시작]**: 자동 포스팅 시작
- **[⏸ 중지]**: 자동 포스팅 중지
- **[🚀 수동 실행]**: 즉시 포스팅

---

## 📊 UI 구성

### 상단
- 📱 **제목**: 네이버밴드 자동 포스팅
- ℹ️ **안내 메시지**

### 메인 영역 (2열)

**좌측: 채팅방 관리**
- 별명 입력란
- URL 입력란
- [✚ 추가] / [🗑 전체 삭제]
- 등록된 채팅방 목록 (체크박스)

**우측: 포스트 관리**
- 포스트 내용 입력란
- [✚ 추가] / [🗑 전체 삭제]
- 등록된 포스트 목록 (체크박스)

### 사이드바 (왼쪽)

**스케줄 설정:**
- 포스팅 간격 (분)
- 랜덤 딜레이 (분)
- 시작 일시
- 종료 일시
- 채팅방 간 대기 (초)

**추가 설정:**
- 포스트 순환 (체크박스)
- 채팅방 순환 (체크박스)

**[💾 설정 저장]** 버튼

### 하단
- **실행 버튼:** [▶ 시작] [⏸ 중지] [🚀 수동 실행]
- **상태 표시:** 실행 상태, 카운트다운, 통계
- **로그 영역:** 실시간 로그 (최근 50개)

---

## 🎨 주요 기능

### 1. 실시간 카운트다운 ⏱️
- 다음 포스팅까지 남은 시간 표시
- 자동 업데이트 (1초마다)

### 2. 체크박스 선택 ✓
- 채팅방 개별 활성화/비활성화
- 포스트 개별 활성화/비활성화

### 3. 실시간 로그 📋
- 모든 활동 기록
- 최근 50개 표시
- 역순 정렬 (최신이 위)

### 4. 통계 표시 📊
- 등록된 채팅방 수
- 등록된 포스트 수

### 5. 반응형 UI 📱
- 화면 크기에 맞게 조정
- 모바일에서도 사용 가능

---

## 🔧 설정 파일

웹 버전도 동일한 설정 파일 사용:
```
config/config.json
```

**데스크톱 버전과 설정 공유 가능!**

---

## 💡 사용 팁

### Tip 1: 다중 탭
여러 브라우저 탭에서 동시 접속 가능  
(단, 포스팅은 하나만 실행)

### Tip 2: 모바일 접속
같은 네트워크의 모바일에서 접속:
```
http://192.168.x.x:8501
```
(서버 실행 시 터미널에 표시됨)

### Tip 3: 자동 새로고침
- 실행 중일 때 자동으로 1초마다 업데이트
- 수동 새로고침: F5 또는 새로고침 버튼

### Tip 4: 터미널 로그
웹 브라우저 외에도 터미널에서 로그 확인 가능

### Tip 5: 포트 변경
다른 포트 사용:
```bash
streamlit run streamlit_app.py --server.port 8502
```

---

## 🆚 데스크톱 vs 웹 버전

| 기능 | 데스크톱 (tkinter) | 웹 (Streamlit) |
|------|-------------------|----------------|
| UI | 전통적인 데스크톱 | 모던한 웹 UI |
| 실행 방법 | .exe 더블클릭 | 브라우저 접속 |
| 설치 | Python 또는 .exe | Python + Streamlit |
| 크로스 플랫폼 | Windows만 (.exe) | Windows/Mac/Linux |
| 모바일 접근 | ❌ | ✅ |
| 자동 새로고침 | 수동 | 자동 (1초) |
| 실시간 로그 | ✅ | ✅ |
| 카운트다운 | ✅ | ✅ |
| 설정 파일 | 공유 가능 | 공유 가능 |

**권장:**
- **Windows 사용자**: 데스크톱 (.exe)
- **Mac/Linux 사용자**: 웹 버전
- **원격 접속 필요**: 웹 버전
- **모던 UI 선호**: 웹 버전

---

## 🚀 실행 예시

### Windows:
```bash
C:\> cd naver-band-auto-poster
C:\naver-band-auto-poster> run_web.bat

========================================
네이버밴드 자동 포스팅 웹 버전 실행
========================================

[1/2] Streamlit 설치 확인 중...
✓ Streamlit이 이미 설치되어 있습니다.

[2/2] 웹 서버 시작 중...

========================================
✓ 웹 브라우저가 자동으로 열립니다!
========================================

브라우저 주소: http://localhost:8501

종료하려면 이 창에서 Ctrl+C를 누르세요.

  You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.1.100:8501
```

---

## 🔍 문제 해결

### 문제 1: Streamlit이 설치되지 않음

**해결:**
```bash
pip install streamlit==1.31.0
```

### 문제 2: 포트가 이미 사용 중

**증상:**
```
Port 8501 is already in use
```

**해결:**
다른 포트 사용:
```bash
streamlit run streamlit_app.py --server.port 8502
```

### 문제 3: 브라우저가 자동으로 열리지 않음

**해결:**
수동으로 열기:
```
http://localhost:8501
```

### 문제 4: 네트워크에서 접속 안됨

**해결:**
방화벽 허용:
```bash
# Windows 방화벽
netsh advfirewall firewall add rule name="Streamlit" dir=in action=allow protocol=TCP localport=8501
```

---

## 🎯 고급 설정

### 서버 설정 커스터마이즈

`.streamlit/config.toml` 파일 생성:

```toml
[server]
port = 8501
headless = true
enableCORS = false
enableXsrfProtection = true

[browser]
serverAddress = "localhost"
gatherUsageStats = false
serverPort = 8501

[theme]
primaryColor = "#FF4B4B"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#262730"
font = "sans serif"
```

---

## 🌐 인터넷에 배포 (선택사항)

### Streamlit Community Cloud

1. GitHub에 코드 푸시
2. https://streamlit.io/cloud 방문
3. "New app" 클릭
4. 저장소 선택
5. `streamlit_app.py` 선택
6. Deploy!

**무료 배포 가능!**

---

## 📦 Docker로 실행 (선택사항)

### Dockerfile 생성:

```dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8501

CMD ["streamlit", "run", "streamlit_app.py", "--server.port=8501", "--server.address=0.0.0.0"]
```

### 빌드 및 실행:

```bash
docker build -t band-poster-web .
docker run -p 8501:8501 band-poster-web
```

---

## 📞 지원

### GitHub Issues
https://github.com/rpaakdi1-spec/naver-band-auto-poster/issues

### Streamlit 문서
https://docs.streamlit.io

---

## ✅ 체크리스트

시작 전 확인:

- [ ] Python 3.8 이상 설치
- [ ] pip 최신 버전
- [ ] requirements.txt 패키지 설치
- [ ] Chrome 브라우저 설치
- [ ] 포트 8501 사용 가능

---

## 🎉 완료!

이제 웹 브라우저에서 네이버밴드 자동 포스팅을 즐기세요!

```bash
run_web.bat
```

**브라우저 주소:** http://localhost:8501

---

**프로젝트:** https://github.com/rpaakdi1-spec/naver-band-auto-poster  
**버전:** v5.0.0 (웹 버전 추가)  
**기술:** Streamlit, Python, Selenium
